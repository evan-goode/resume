{
  "dependencies": {
    "parcel": "^1.9.7"
  },
  "scripts": {
    "start": "npx parcel source/index.pug --cache-dir build/cache/ --out-dir build/",
    "build": "npx parcel build source/index.pug --cache-dir build/cache/ --out-dir build/"
  },
  "devDependencies": {
    "node-sass": "^4.9.3",
    "pug": "^2.0.3"
  }
}
